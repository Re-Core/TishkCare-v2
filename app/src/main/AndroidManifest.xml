<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.recore.tishkcare">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitys.Activtys.SetAppointmentActivity"></activity>
        <activity
            android:name=".Activitys.Activtys.SettingActivity"
            android:label="Setting" />
        <activity android:name=".Activitys.Activtys.AppointmentDetailActivity" />
        <activity android:name=".Activitys.Activtys.DoctorDetailActivity" />
        <activity android:name=".Activitys.Activtys.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activitys.Activtys.MainActivity" />
        </activity>
        <activity android:name=".Activitys.Activtys.SignUpInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>